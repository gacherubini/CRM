# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
test-tmp*/
.pytest-tmp/

# Scratch / hotpatch / probes de sessão (nunca versionar)
tmp/

# Docker / dados locais
pg_data/
n8n_data/

# Dados locais do Motor (screenshots + storage_state com cookies de sessão) — NUNCA versionar
motor-simulacao/data/
data/screenshots/
data/storage_state/

# Bancos locais
*.db

# Ambiente
.env
deploy/fly/.env.production.local
deploy/fly/3vm/.secrets.local
deploy/fly/3vm/.evolution_key.local
# Workflow gerado com tokens reais (prepare-workflow.ps1) — NUNCA versionar
deploy/fly/3vm/workflow-fly.ready.json
deploy/fly/3vm/workflow-fly-test.ready.json

# PDFs/artefatos gerados de manuais (fonte é docs/tutorial-*.md)
output/

# scripts de patch one-off do n8n (nao versionar)
n8n/_*.py

# worktrees efêmeros de agentes (Claude Code)
.claude/

# one-off fly/n8n patch scripts
deploy/fly/_*.js
deploy/fly/_*.py
motor-simulacao/scripts/_diag*.py
motor-simulacao/scripts/diag_bradesco_recaptcha.py

